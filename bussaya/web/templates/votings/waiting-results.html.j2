{% extends '/base/default-dashboard.html.j2' %}

{% block content %}
<div class="card bg-base-100 shadow my-4 h-[80vh]">
  <div class="card-body items-center justify-center text-center">
    <div class="text-xl font-semibold">
      <i class="fa-solid fa-gears fa-2x"></i>
      <p>Waiting results, See you next year.</p>
      <div>
      {% if 'lecturer' in current_user.roles %}
        <a class="btn btn-primary" href="{{ url_for('dashboard.index') }}">Go to Dashbard</a>
      {% endif %}
      </div>
    </div>
  </div>
</div>
{% endblock %}
